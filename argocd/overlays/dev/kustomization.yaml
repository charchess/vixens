apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - cilium-lb-app.yaml        # Cilium L2 Announcements + LB IPAM (wave -2)
  - metallb-app.yaml          # MetalLB Helm chart (wave 0) - TO BE REMOVED
  - metallb-config-app.yaml   # MetalLB config (wave -1) - TO BE REMOVED
  # Future: Add more ArgoCD Applications here as we deploy services
  # - traefik-app.yaml
  # - cert-manager-app.yaml
  # - synology-csi-app.yaml
