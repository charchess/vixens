apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd
resources:
  # This path is relative to this kustomization.yaml file.
  # It points to the 'adoption' manifest I just created.
  - ../../../apps/00-infra/argocd/app.yaml
