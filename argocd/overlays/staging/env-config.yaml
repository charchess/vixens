---
# Environment-specific configuration for staging
# Used by Kustomize replacements to inject values into app templates
# This ConfigMap is NOT deployed to the cluster - it's used at build time only
apiVersion: v1
kind: ConfigMap
metadata:
  name: env-config
  namespace: argocd
  annotations:
    kustomize.config.k8s.io/behavior: create
    description: "Environment configuration for staging - used at build time"
data:
  # ============================================================================
  # Environment Identification
  # ============================================================================
  environment: "staging"
  git_branch: "staging"
  cluster_name: "vixens-staging"

  # ============================================================================
  # Domain Configuration
  # ============================================================================
  domain_suffix: "stg"
  base_domain: "truxonline.com"
  full_domain: "stg.truxonline.com"

  # ============================================================================
  # Network Configuration - VLAN Services (210)
  # ============================================================================
  vlan_id: "210"
  vlan_subnet: "192.168.210.0/24"
  vlan_gateway: "192.168.210.1"

  # Internal VLAN (same for all environments)
  vlan_internal: "111"
  vlan_internal_subnet: "192.168.111.0/24"

  # ============================================================================
  # LoadBalancer IP Pools (Cilium L2 Announcements)
  # ============================================================================
  # Assigned pool: .70-.79 (static IPs for specific services)
  lb_pool_assigned_start: "192.168.210.70"
  lb_pool_assigned_end: "192.168.210.79"

  # Auto pool: .80-.89 (dynamic allocation by Cilium)
  lb_pool_auto_start: "192.168.210.80"
  lb_pool_auto_end: "192.168.210.89"

  # Full pool range
  lb_pool_start: "192.168.210.70"
  lb_pool_end: "192.168.210.89"

  # ============================================================================
  # Service LoadBalancer IPs (from assigned pool .70-.79)
  # ============================================================================
  traefik_lb_ip: "192.168.210.70"
  argocd_server_ip: "192.168.210.71"
  # Reserved for future services: .72-.79

  # ============================================================================
  # Cluster Configuration
  # ============================================================================
  # Kubernetes API VIP on VLAN 111
  cluster_vip: "192.168.111.190"
  cluster_endpoint: "https://192.168.111.190:6443"

  # ============================================================================
  # Control Plane Nodes (VLAN 111 IPs) - TBD
  # ============================================================================
  # node_xxx_ip: "192.168.111.192"
  # node_yyy_ip: "192.168.111.194"
  # node_zzz_ip: "192.168.111.193"

  # ============================================================================
  # Application-Specific Configuration
  # ============================================================================
  # Talos version
  talos_version: "v1.11.5"

  # Kubernetes version
  kubernetes_version: "v1.34.0"

  # Cilium version
  cilium_version: "v1.18.3"

  # ArgoCD version
  argocd_version: "v7.7.7"

  # ============================================================================
  # Resource Limits (closer to production)
  # ============================================================================
  resource_tier: "staging"  # development | staging | production
