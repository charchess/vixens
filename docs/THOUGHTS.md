necessité reelle d'avoir un prod-stable ? plutot que le head au vu de mon
utilisation ? (pas de dev)

ou modification du fonctionnement de la dev ? pour vraiment valider les
elements pertinents uniquement ? (cluster fresh install, sans auto sync ?
avec sync manuelle des applis qu'on veut tester ou autosync des applications
uniquement ?)

un just permettant de passer le prod-stable vers prod-working (revert de
l'etat du cluster) et un bougeant le prod-working vers prod-stable
(validation de foncitonnement du cluster demandant confirmation)

mise en place chap ? et hosts ?

preparer un just report pour la cheffe de projet ? qui genere tout (report
cdp, dashboard, status ...

