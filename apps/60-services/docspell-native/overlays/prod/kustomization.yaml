apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: services

resources:
  - ../../base
  - ingress.yaml

patches:
  - path: patch-infisical-secrets.yaml

labels:
  - pairs:
      environment: prod