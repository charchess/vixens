apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: services

resources:
  - ../../base

patches:
  - path: patch-infisical-secrets.yaml
  - path: ingress.yaml
    target:
      kind: Ingress
      name: docspell-restserver

labels:
  - pairs:
      environment: prod
