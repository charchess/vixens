---
apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: services
  labels:
    app: openclaw
spec:
  type: NodePort
  ports:
    - port: 18789
      targetPort: 18789
      nodePort: 18789
      protocol: TCP
      name: http
  selector:
    app: openclaw
