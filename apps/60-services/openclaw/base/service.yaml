---
apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: services
  labels:
    app: openclaw
spec:
  ports:
    - port: 18789
      targetPort: 18789
      name: http
  selector:
    app: openclaw
