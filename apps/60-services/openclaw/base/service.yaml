---
apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: services
  labels:
    app: openclaw
spec:
  ports:
    - port: 8080
      targetPort: http
      name: http
  selector:
    app: openclaw
