---
apiVersion: v1
kind: Service
metadata:
  name: valkey-hermes
  namespace: services
  labels:
    app.kubernetes.io/name: valkey-hermes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: valkey-hermes
  ports:
    - name: valkey
      port: 6379
      targetPort: valkey
      protocol: TCP
