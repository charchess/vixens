apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: template-app-litestream-config
    files:
      - litestream.yml

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - infisical-secret.yaml
  - litestream-secret.yaml