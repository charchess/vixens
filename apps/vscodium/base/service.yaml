apiVersion: v1
kind: Service
metadata:
  name: vscodium-svc
  namespace: vscodium
  labels:
    app: vscodium
spec:
  type: ClusterIP
  selector:
    app: vscodium
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 3000  # openvscode-server listens on 3000
