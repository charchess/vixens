apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vscodium # Ensure vscodium resources are deployed in vscodium namespace
resources:
  - ../../base
  - ingress.yaml
  - ingress-http-redirect.yaml
  - infisical-vscodium-password.yaml
