apiVersion: apps/v1
kind: Deployment
metadata:
  name: vscodium
spec:
  template:
    spec:
      tolerations: # TODO a porter sur la base ...
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
