---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mealie-litestream-config
data:
  litestream.yml: |
    dbs:
      - path: /app/data/mealie.db
        replicas:
          - url: s3://$LITESTREAM_BUCKET/mealie.db
            endpoint: $LITESTREAM_ENDPOINT
    addr: ":9090"
