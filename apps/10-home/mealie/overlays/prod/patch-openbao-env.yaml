---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mealie-secrets-sync
  namespace: mealie
spec:
  dataFrom:
  - extract:
      key: vixens/prod/apps/10-home/mealie
  target:
    template:
      metadata:
        labels:
          app.kubernetes.io/managed-by: external-secrets
        annotations:
          vixens.io/secret-source: openbao
