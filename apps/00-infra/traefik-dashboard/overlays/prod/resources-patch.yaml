---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: traefik-dashboard
spec:
  template:
    spec:
      containers:
        - name: traefik-dashboard
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 64Mi
