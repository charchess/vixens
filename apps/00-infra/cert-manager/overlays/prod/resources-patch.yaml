apiVersion: apps/v1
kind: Deployment
metadata:
  name: cert-manager-config
spec:
  template:
    spec:
      containers:
      - name: cert-manager-config
        resources:
          limits:
            cpu: 500m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 64Mi
