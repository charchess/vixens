---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kyverno
resources:
  - ../../base
labels:
  - includeSelectors: false
    pairs:
      environment: prod
# Production: HA configuration with 3 replicas per controller
# Uncomment and use values-ha.yaml when ready for HA
# patches:
#   - path: values-ha.yaml
