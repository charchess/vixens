---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kyverno
resources:
  - ../../base
labels:
  - includeSelectors: false
    pairs:
      environment: prod
# Production: HA configuration with 3 replicas per controller
# See values/ha.yaml for HA configuration reference
# To enable HA, merge values into base/values.yaml or use valuesInline
