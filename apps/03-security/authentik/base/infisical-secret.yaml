apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: authentik-secrets-sync
spec:
  secretName: authentik-secrets
  secretPath: /kubernetes/authentik
  secretNamespace: auth
  resyncInterval: 60
