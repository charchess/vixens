---
apiVersion: v1
kind: Service
metadata:
  name: authentik
  namespace: auth
  labels:
    app: authentik
spec:
  selector:
    app: authentik-server
  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP
