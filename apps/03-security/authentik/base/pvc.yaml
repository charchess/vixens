apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-data
  namespace: auth
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synelia-iscsi-retain
  resources:
    requests:
      storage: 10Gi # Adjust as needed
