---
apiVersion: v1
kind: Service
metadata:
  name: vikunja
  namespace: "99-test"
  labels:
    app.kubernetes.io/name: vikunja
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: vikunja
