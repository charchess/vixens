apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: vixens-dev-l2
  namespace: metallb-system
spec:
  ipAddressPools:
    - vixens-dev-assigned
    - vixens-dev-auto
  # No interfaces specified = announce on all interfaces (including VLAN 208)
