apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: vixens-dev-assigned
  namespace: metallb-system
spec:
  addresses:
    - 192.168.208.70-192.168.208.79  # Pool for assigned/static IPs (Traefik, ArgoCD, etc.)
  autoAssign: false  # Manual assignment only
---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: vixens-dev-auto
  namespace: metallb-system
spec:
  addresses:
    - 192.168.208.80-192.168.208.89  # Pool for auto-assigned IPs
  autoAssign: true  # Automatic assignment for LoadBalancer services
