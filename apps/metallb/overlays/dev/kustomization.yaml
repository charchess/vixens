apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metallb-system

resources:
  - ../../base  # Include base resources (namespace with PodSecurity labels)
  - ipaddresspool.yaml
  - l2advertisement.yaml
