apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metallb-system

resources:
  - namespace.yaml
  # Note: Helm chart deployed via argocd/overlays/dev/metallb-app.yaml
  # This base only provides namespace with PodSecurity labels + shared config
