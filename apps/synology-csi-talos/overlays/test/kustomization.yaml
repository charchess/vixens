---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: synology-csi

resources:
  - ../../base

labels:
  - pairs:
      vixens.lab/environment: test
    includeTemplates: true

patches:
  # Patch le Secret pour test
  - target:
      kind: Secret
      name: synology-csi-secret
    path: secret-patch.yaml
