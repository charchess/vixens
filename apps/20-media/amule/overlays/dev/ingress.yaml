---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: amule
  namespace: downloads
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  rules:
    - host: amule.dev.truxonline.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: amule
                port:
                  number: 4711
  tls:
    - hosts:
        - amule.dev.truxonline.com
      secretName: amule-tls-dev