apiVersion: v1
kind: Service
metadata:
  name: hydrus-client
  labels:
    app: hydrus-client
spec:
  selector:
    app: hydrus-client
  ports:
    - protocol: TCP
      port: 5800
      targetPort: 5800
