apiVersion: apps/v1
kind: Deployment
metadata:
  name: booklore
spec:
  template:
    spec:
      containers:
      - name: booklore
        resources:
          requests:
            cpu: "15m"
            memory: "1848Mi"
          limits:
            cpu: "548m"
            memory: "5545Mi"
