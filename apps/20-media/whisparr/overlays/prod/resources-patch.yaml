apiVersion: apps/v1
kind: Deployment
metadata:
  name: whisparr
spec:
  template:
    spec:
      containers:
      - name: whisparr
        resources:
          requests:
            cpu: "15m"
            memory: "120Mi"
          limits:
            cpu: "15m"
            memory: "120Mi"
