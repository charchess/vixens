---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lazylibrarian
  namespace: media
  labels:
    app: lazylibrarian
spec:
  selector:
    matchLabels:
      app: lazylibrarian
  namespaceSelector:
    matchNames:
      - media
  endpoints:
    - port: metrics
      targetPort: 9090
      path: /metrics
      interval: 60s
