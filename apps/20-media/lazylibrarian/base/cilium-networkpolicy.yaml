---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: lazylibrarian
spec:
  endpointSelector:
    matchLabels:
      app: lazylibrarian
  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: traefik
      toPorts:
        - ports:
            - port: "5299"
              protocol: TCP
  egress:
    - {}
