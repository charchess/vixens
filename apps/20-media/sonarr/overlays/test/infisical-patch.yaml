apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

patches:
  - target:
      kind: InfisicalSecret
      name: sonarr-secrets-sync
    patch: |
      - op: replace
        path: /spec/secretPath
        value: /kubernetes/sonarr/test
