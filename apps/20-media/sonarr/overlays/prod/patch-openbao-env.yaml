---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarr-secrets-sync
  namespace: media
spec:
  dataFrom:
  - extract:
      key: vixens/prod/apps/20-media/sonarr
  target:
    template:
      metadata:
        labels:
          app.kubernetes.io/managed-by: external-secrets
        annotations:
          vixens.io/secret-source: openbao
