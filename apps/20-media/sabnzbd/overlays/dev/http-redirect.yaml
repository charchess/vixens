---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: media # The namespace must be the same as your Ingress
spec:
  redirectScheme:
    scheme: https
    permanent: true
