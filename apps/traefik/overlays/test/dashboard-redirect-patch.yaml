apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard-redirect
  namespace: traefik
spec:
  routes:
    - match: Host(`traefik.test.truxonline.com`) && Path(`/`)
