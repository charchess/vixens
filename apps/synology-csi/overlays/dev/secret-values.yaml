# apps/synology-csi/overlays/dev/secret-values.yaml
apiVersion: v1
kind: Secret
metadata:
  name: synology-csi-credentials
  namespace: synology-csi
type: Opaque
stringData:
  config.yaml: |
    # config for Synology CSI Driver
    clients:
      - host: "https://192.168.1.10:5001"
        username: "admin"
        password: "password"
        # Optional: specify volume/lun name prefix
        # volume_name_prefix: "my-prefix"
