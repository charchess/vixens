---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: netvisor-daemon
spec:
  template:
    spec:
      containers:
        - name: daemon
          env:
            - name: SCANOPY_SERVER_URL
              value: "https://netvisor.truxonline.com"
