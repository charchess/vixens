apiVersion: apps/v1
kind: Deployment
metadata:
  name: external-dns-unifi-secrets
spec:
  template:
    spec:
      containers:
      - name: external-dns-unifi-secrets
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 20m
            memory: 128Mi
