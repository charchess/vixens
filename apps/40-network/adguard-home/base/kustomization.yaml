---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: networking
resources:
  - middleware.yaml
  - statefulset.yaml
  - service.yaml
  # pvc.yaml removed - now managed by StatefulSet volumeClaimTemplates
  - ingressroute-dns.yaml
  - infisical-secret.yaml
  - litestream-config.yaml
