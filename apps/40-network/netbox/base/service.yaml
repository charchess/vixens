apiVersion: v1
kind: Service
metadata:
  name: netbox
  labels:
    app: netbox
spec:
  type: ClusterIP
  selector:
    app: netbox
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
