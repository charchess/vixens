apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: netbox-secrets-sync
spec:
  secretName: netbox-secrets
  secretPath: /kubernetes/netbox
  secretNamespace: networking
  resyncInterval: 60
