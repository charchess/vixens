---
apiVersion: v1
kind: Service
metadata:
  name: mail-gateway-svc
  namespace: mail-gateway
spec:
  type: ExternalName
  externalName: roundcube.mail.svc.cluster.local
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
