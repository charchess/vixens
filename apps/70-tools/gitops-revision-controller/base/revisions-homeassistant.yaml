apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: homeassistant-revision
  namespace: tools
spec:
  managedSecretReference:
    secretName: homeassistant-revision
    secretNamespace: tools
    creationPolicy: Owner
    secretType: Opaque
    # Templates/Annotations are not directly supported by InfisicalSecret to be applied on the GENERATED secret easily unless we use a specific feature or if the Operator copies them.
    # The Infisical Operator (v1alpha1) typically copies labels/annotations from the InfisicalSecret to the ManagedSecret?
    # Let's verify documentation or assume standard behavior.
    # If not, we might need a workaround (e.g. creating the Secret wrapper manually or using a reflector).
    # Update: Recent Infisical Operator versions support `managedSecretReference.template` or similar.
    # checking `apps/01-storage/synology-csi/base/infisical/client-info.yaml` for examples.
