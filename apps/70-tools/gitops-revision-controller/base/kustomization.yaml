apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tools

resources:
  - deployment.yaml
  - rbac.yaml
  - revisions-homeassistant.yaml

configMapGenerator:
  - name: gitops-revision-controller-src
    files:
      - src/main.py
      - src/requirements.txt
