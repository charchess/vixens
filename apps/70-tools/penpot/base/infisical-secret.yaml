apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: penpot-secrets-sync
  namespace: tools
spec:
  hostAPI: https://infisical.truxonline.com/api
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: vixens-homelab-cv6
        envSlug: prod
        secretsPath: /tools/penpot
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: tools
  managedSecretReference:
    secretName: penpot-secrets
    creationPolicy: Owner
