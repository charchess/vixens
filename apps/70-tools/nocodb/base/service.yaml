---
apiVersion: v1
kind: Service
metadata:
  name: nocodb
  namespace: tools
  labels:
    app.kubernetes.io/name: nocodb
spec:
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: nocodb
