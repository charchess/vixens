apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tools

resources:
  - database.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - middleware-redirect.yaml

labels:
  - pairs:
      app: linkwarden
