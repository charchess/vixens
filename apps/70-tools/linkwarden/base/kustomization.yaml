---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Don't set namespace - let each resource define its own
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
labels:
  - pairs:
      app: linkwarden
