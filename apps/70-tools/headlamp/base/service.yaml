apiVersion: v1
kind: Service
metadata:
  name: headlamp
  labels:
    app: headlamp
spec:
  type: ClusterIP
  selector:
    app: headlamp
  ports:
    - name: http
      port: 4466
      targetPort: 4466
      protocol: TCP
