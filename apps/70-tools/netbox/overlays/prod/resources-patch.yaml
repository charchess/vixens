apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbox
spec:
  template:
    spec:
      containers:
    - name: netbox
          resources:
            limits:
              cpu: 500m
              memory: 1173Mi
            requests:
              cpu: 11m
              memory: 640Mi
