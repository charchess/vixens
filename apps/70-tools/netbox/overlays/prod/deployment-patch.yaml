apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbox
spec:
  template:
    spec:
      containers:
        - name: netbox
          env:
            - name: ALLOWED_HOSTS
              value: "netbox.truxonline.com"
