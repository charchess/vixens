apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: netbox-ingress
  namespace: tools
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
    traefik.ingress.kubernetes.io/router.entrypoints: "web, websecure"
    traefik.ingress.kubernetes.io/router.middlewares: tools-netbox-redirect-to-https@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: "netbox.test.truxonline.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: netbox
                port:
                  number: 8080
  tls:
    - hosts:
        - "netbox.test.truxonline.com"
      secretName: netbox-tls
