apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: argocd-image-updater-secret-sync
  namespace: tools
spec:
  hostAPI: http://192.168.111.69:8085
  resyncInterval: 60
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: vixens
        envSlug: dev # Default, patched by overlay
        secretsPath: /apps/70-tools/argocd-image-updater
        secretName: infisical-universal-auth
        secretNamespace: argocd
  managedSecretReference:
    secretName: argocd-image-updater-secret
    secretNamespace: tools
    creationPolicy: Owner
---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: ssh-git-creds-sync
  namespace: tools
spec:
  hostAPI: http://192.168.111.69:8085
  resyncInterval: 60
  authentication:
    universalAuth:
      secretsScope:
        projectSlug: vixens
        envSlug: dev # Default, patched by overlay
        secretsPath: /apps/70-tools/argocd-image-updater/ssh
        secretName: infisical-universal-auth
        secretNamespace: argocd
  managedSecretReference:
    secretName: ssh-git-creds
    secretNamespace: tools
    creationPolicy: Owner