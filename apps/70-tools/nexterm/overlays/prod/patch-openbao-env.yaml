---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nexterm-secrets-sync
  namespace: tools
spec:
  dataFrom:
  - extract:
      key: vixens/prod/apps/70-tools/nexterm
  target:
    template:
      metadata:
        labels:
          app.kubernetes.io/managed-by: external-secrets
        annotations:
          vixens.io/secret-source: openbao
