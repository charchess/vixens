---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexterm-data
  labels:
    app: nexterm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-delete
  resources:
    requests:
      storage: 2Gi
