apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - ../../base

# Dev environment doesn't need specific patches yet
# Future: could add dev-specific log levels or resource limits
