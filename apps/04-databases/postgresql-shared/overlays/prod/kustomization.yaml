apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: databases

resources:
  - ../../base

patchesStrategicMerge:
  - patch-credentials-env.yaml

commonLabels:
  environment: prod
