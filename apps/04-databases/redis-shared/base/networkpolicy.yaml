---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-shared
spec:
  podSelector:
    matchLabels:
      app: redis-shared
  policyTypes:
    - Ingress
    - Egress
  ingress: []
  egress:
    # Allow all egress (homelab: DNS, inter-app, external APIs)
    - {}
