apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-shared
spec:
  template:
    spec:
      priorityClassName: vixens-critical
      containers:
      - name: redis
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
