apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: databases

labels:
  - pairs:
      environment: prod
    includeSelectors: false

patches:
  - path: resources-patch.yaml
    target:
      kind: Deployment
      name: redis-shared

resources:
  - ../../base

