---
clusterName: "vixens-prod"

enablePlatformPlaybooks: true
enableHolmesGPT: true # On le gère à part
holmes:
  enabled: false
enabledManagedConfiguration: true

globalConfig:
  signing_key: "{{ env.ROBUSTA_SIGNING_KEY }}"
  account_id: "{{ env.ROBUSTA_ACCOUNT_ID }}"
  prometheus_url: "http://prometheus-server.monitoring.svc.cluster.local"

sinksConfig:
  - robusta_sink:
      name: robusta_ui_sink
      token: "{{ env.ROBUSTA_UI_TOKEN }}"
  - discord_sink:
      name: main_discord_sink
      url: "{{ env.DISCORD_WEBHOOK_URL }}"

runner:
  # PolicyException bypasses Kyverno sizing-mutate. Explicit resources set here.
  labels:
    vixens.io/sizing.runner: B-small
  tolerations:
    - key: node-role.kubernetes.io/control-plane
      operator: Exists
      effect: NoSchedule
  resources:
    requests:
      memory: 384Mi
      cpu: 250m
    limits:
      memory: 768Mi
      cpu: 500m
  sendAdditionalTelemetry: true
  additional_env_froms:
    - secretRef:
        name: robusta-secrets
