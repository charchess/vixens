apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-ingress
spec:
  template:
    spec:
      containers:
      - name: prometheus-ingress
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
          requests:
            cpu: 20m
            memory: 128Mi
