---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - ../../base
  # prometheus-ingress.yaml managed by separate ArgoCD app

patchesStrategicMerge:
  - patch-alertmanager-command.yaml

commonLabels:
  environment: dev
