---
apiVersion: v1
kind: Service
metadata:
  name: hubble-ui
  namespace: monitoring
  labels:
    app: hubble-ui
spec:
  selector:
    app: hubble-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
