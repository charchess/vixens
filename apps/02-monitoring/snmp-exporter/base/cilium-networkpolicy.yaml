---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: snmp-exporter
  namespace: monitoring
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: snmp-exporter
  ingress:
    - fromEntities:
        - host
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: monitoring
      toPorts:
        - ports:
            - port: "9116"
              protocol: TCP
  egress:
    # init-vmscrape → kube-apiserver (patch VMStaticScrape runtime targets)
    - toEntities:
        - kube-apiserver
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # snmp-exporter → SNMP devices on local network (port 161 UDP)
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "161"
              protocol: UDP
