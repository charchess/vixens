---
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: vm-alertmanager-secrets-sync
  namespace: monitoring
spec:
  authentication:
    universalAuth:
      secretsScope:
        envSlug: prod
