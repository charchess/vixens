---
apiVersion: v1
kind: Service
metadata:
  name: promtail
spec:
  ports:
    - name: http-metrics
      port: 9080
      protocol: TCP
      targetPort: 9080
  selector:
    app: promtail
  type: ClusterIP
