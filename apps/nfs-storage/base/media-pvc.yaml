apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: nfs-storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 1Mi
