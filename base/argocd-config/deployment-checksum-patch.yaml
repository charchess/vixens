# base/argocd-config/deployment-checksum-patch.yaml
---
# Ce patch ajoute une annotation vide qui sera remplie par Kustomize
apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
spec:
  template:
    metadata:
      annotations:
        checksum/config: $(CHECKSUM)
