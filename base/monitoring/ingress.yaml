# base/monitoring/ingress.yaml
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus-ingress
  namespace: monitoring
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-gandi-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - prometheus.truxonline.com
      secretName: prometheus-tls
  rules:
    - host: prometheus.truxonline.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                # La cible doit être EXACTEMENT ce nom
                name: prometheus-server
                port:
                  # Le port doit être EXACTEMENT ce numéro
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: monitoring
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-gandi-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - grafana.truxonline.com
      secretName: grafana-tls
  rules:
    - host: grafana.truxonline.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                # Le nom de service simple et correct
                name: grafana
                port:
                  number: 80
