# base/metallb/kustomization.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metallb-system

resources:
  - namespace.yaml
  - metallb-native.yaml
  - config.yaml
  - cluster-selector.yaml  # ⭐ Logique cluster-based

commonLabels:
  app.kubernetes.io/part-of: metallb
  managed-by: gitops
  component: load-balancer
