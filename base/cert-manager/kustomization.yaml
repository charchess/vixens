# base/cert-manager/kustomization.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - clusterissuer.yaml
  - secret-gandi-credentials.yaml  # si tu le gères en clair (ou SealedSecret)
