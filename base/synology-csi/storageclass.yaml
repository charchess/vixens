# base/synology-csi-config/storageclass.yaml
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: synology-iscsi-sc
provisioner: csi.synology.com
parameters:
  protocol: iscsi
  fsType: ext4
volumeBindingMode: WaitForFirstConsumer
