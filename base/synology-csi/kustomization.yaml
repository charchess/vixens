# base/synology-csi/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: synology-csi

resources:
  - secret.yaml
  - rbac.yaml
  - controller.yaml
  - node.yaml
  # --- AJOUT IMPORTANT ---
  # On dit à Kustomize de lire aussi le contenu de ce dossier
  - snapshotter
