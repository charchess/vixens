# base/synology-csi/kustomization.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: synology-csi

resources:
- upstream          # répertoire local
- namespace.yaml

patches:
- path: client-info-secret.yaml
  target:
    kind: Secret
    name: client-info-secret
